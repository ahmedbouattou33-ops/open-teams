# OpenTeams Part 1 Report

This is a complete source archive of the current project, excluding only secrets and regenerable runtime artifacts.

## Excluded intentionally
- `.env`, `.env.local`, `.env.production`: real credential values are not exported.
- Every `*/keys/` directory: JWT/private/public key material is not exported.
- `node_modules/`: regenerated with `pnpm install`.
- `dist/`, `.next/`, `.turbo/`, and `*/src/generated/`: generated output/cache regenerated by build and Prisma commands.

## Secret status
Real working `.env` values and key contents are NOT present in this archive. The `.env.example` files, when present, are templates only.

## Manifest verification
Files in allowlist before manifest/report: 207.
Final archive entries/files: 209, including `PART1_MANIFEST.txt` and `PART1_REPORT.md`.
Normalized exact comparison after final rebuild: `MISSING_NORMALIZED=0`, `EXTRA_NORMALIZED=0`, `MANIFEST_MATCH=YES`.
ZIP integrity check: passed with `unzip -tq`.

## Installation
Extract the archive, enter `open-teams`, run `pnpm install`, copy each `.env.example` to `.env`, configure new hosts/ports, run Prisma generate for Prisma services, then run `docker compose up -d --build`.
